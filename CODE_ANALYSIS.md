# X-piggybacking 程式碼分析報告

## 目錄
1. [系統架構概覽](#系統架構概覽)
2. [資料流程](#資料流程)
3. [已實作的模組](#已實作的模組)
4. [未實作的功能](#未實作的功能)
5. [實作優先順序建議](#實作優先順序建議)

---

## 系統架構概覽

### 核心流程（Pipeline）

```
Google Sheets → Apify Scraper → Keyword Matching → Template Selection
                                        ↓
                                  Reply Generation
                                        ↓
                                   Human Review (optional)
                                        ↓
                                   Post to X API
                                        ↓
                                  Log to Sheets
```

### 專案結構

```
x_auto/
├── config/         # 環境配置和憑證管理
├── sheets/         # Google Sheets 讀寫
├── scrapers/       # Apify 整合（不在 x_auto 下，在根目錄）
├── matcher/        # 關鍵字匹配和評分
├── reply_engine/   # 回覆生成
├── x_api/          # X API 客戶端
├── workflow/       # 主要流程編排
└── utils/          # 工具函數（logger, rate_limit, id_tracker）
```

---

## 資料流程

### 1. 輸入資料（來自 Google Sheets）

#### Profile Sheet ("Researcher")
- 目前有 **49 筆** 研究員資料
- 欄位結構：
  ```
  - Company: 公司名稱
  - Name: 研究員姓名
  - Role: 職位（可能為空）
  - Handle: X 帳號（如 @LafaMarino）
  - X (link): 完整 X URL
  ```

#### Keywords Sheet (尚未確認結構)
- 預期包含：
  ```
  - keyword: 關鍵字文字
  - weight: 權重分數
  - template_id: 關聯的模板ID（可選）
  ```

#### Templates Sheet (尚未確認結構)
- 預期包含：
  ```
  - template_id: 模板識別碼
  - template_text: 回覆模板文字
  - category: 分類或使用場景
  ```

### 2. Apify 抓取資料結構

根據 `poc_apify_demo.py`，Apify 返回的貼文資料包含：
```python
{
    "id" or "postId": "貼文ID",
    "text" or "postText": "貼文內容",
    "favoriteCount": 按讚數,
    "replyCount": 回覆數,
    "retweetCount": 轉發數,
    # ... 其他元資料
}
```

### 3. 處理流程資料轉換

```
Profile URL → Apify → Raw Posts → Filter (ID tracking) → New Posts
                                                              ↓
                                                    Keyword Matching
                                                              ↓
                                                       Score Calculation
                                                              ↓
                                                    Template Selection
                                                              ↓
                                                     Reply Generation
                                                              ↓
                                                   (Optional) Human Review
                                                              ↓
                                                        Post to X
                                                              ↓
                                                      Log to Sheets
```

---

## 已實作的模組

### ✅ 完整實作

#### 1. **Apify 客戶端** (`scrapers/apify_client.py`)
- ✓ 功能：呼叫 Apify actor 抓取 X 貼文
- ✓ API 整合：使用 `scraper_one/x-profile-posts-scraper`
- ✓ 錯誤處理：HTTP 狀態碼檢查和錯誤訊息
- **狀態：可直接使用**

#### 2. **X API 客戶端** (`x_auto/x_api/x_client.py`)
- ✓ 功能：發布回覆到 X (Twitter)
- ✓ 支援：OAuth 1.0a 和 OAuth 2.0
- ✓ 認證：自動選擇認證方式
- ✓ 錯誤處理：包含 HTTP 錯誤和 JSON 解析錯誤
- **狀態：可直接使用**

#### 3. **Google Sheets 客戶端** (`x_auto/sheets/client.py`)
- ✓ 功能：讀寫 Google Sheets
- ✓ 認證：使用 Service Account
- ✓ 方法：
  - `read_records()` - 讀取工作表為字典列表
  - `append_row()` - 新增一列
  - `get_sheet()` - 獲取特定工作表
- **狀態：可直接使用**

#### 4. **關鍵字匹配器** (`x_auto/matcher/keyword_matcher.py`)
- ✓ `match_keywords()` - 在貼文中尋找關鍵字（大小寫不敏感）
- ✓ `score_matches()` - 計算匹配分數（基於權重 + 互動數）
- **狀態：可直接使用**

#### 5. **工具函數** (`x_auto/utils/`)

##### `logger.py`
- ✓ `get_logger()` - 創建標準化的 logger
- **狀態：可直接使用**

##### `rate_limit.py`
- ✓ `throttle()` - 裝飾器，用於限制函數呼叫頻率
- **狀態：可直接使用**

##### `id_tracker.py`
- ✓ `get_last_seen_id()` - 獲取最後處理的貼文ID
- ✓ `update_last_seen_id()` - 更新最後處理的貼文ID
- ⚠️ **限制：僅在記憶體中，程式重啟後會遺失**
- **狀態：可用但需要改進（持久化存儲）**

#### 6. **配置管理** (`x_auto/config/settings.py`)
- ✓ `load_environment()` - 載入 .env 檔案
- ✓ `get_api_credentials()` - 獲取 API 憑證
- **狀態：可直接使用**

### ⚠️ 部分實作

#### 1. **Reply Once 工具** (`reply_once.py`)
- ✓ 功能：單次回覆特定貼文
- ✓ 命令列介面
- ✓ URL/ID 解析
- **狀態：可用於測試和手動回覆**

#### 2. **Apify Demo** (`poc_apify_demo.py`)
- ✓ 功能：演示 Apify 抓取功能
- **狀態：可用於測試**

---

## 未實作的功能

### ❌ 主要流程中的缺口

#### 1. **Pipeline 中的三個 Stub 函數**

##### `filter_already_processed()` - 🔴 高優先級
**位置：** `x_auto/workflow/pipeline.py:80`

**目的：** 過濾已處理過的貼文，避免重複回覆

**當前狀態：**
```python
raise NotImplementedError("Implement ID tracking to filter already processed posts.")
```

**需要實作：**
- 從持久化存儲（如 Google Sheets 或本地檔案）讀取已處理的貼文ID
- 過濾掉已處理的貼文
- 可能需要整合 `id_tracker.py` 模組

**依賴：**
- 需要決定存儲方式（Google Sheets vs JSON file vs SQLite）

---

##### `format_log_row()` - 🟡 中優先級
**位置：** `x_auto/workflow/pipeline.py:93`

**目的：** 將貼文、回覆和 API 回應格式化為 Google Sheets 的一列

**當前狀態：**
```python
raise NotImplementedError("Implement log formatting for Google Sheets.")
```

**需要實作：**
- 定義日誌 Sheet 的欄位結構
- 將貼文資料、回覆文字、API 回應轉換為列表格式
- 包含時間戳記、狀態等資訊

**範例輸出格式：**
```python
[
    timestamp,           # 時間
    profile_handle,      # 來源帳號
    post_id,            # 貼文ID
    post_text[:100],    # 貼文內容（截斷）
    reply_text,         # 回覆內容
    status,             # 成功/失敗
    response_id,        # X API 回應的回覆ID
    error_message       # 錯誤訊息（如有）
]
```

---

##### `requires_human_approval()` - 🟢 低優先級
**位置：** `x_auto/workflow/pipeline.py:108`

**目的：** 決定回覆是否需要人工審核

**當前狀態：**
```python
raise NotImplementedError("Implement human approval decision logic.")
```

**需要實作：**
- 定義需要審核的條件（例如：敏感關鍵字、低分數、新類型等）
- 可選：整合 Slack/Telegram 通知
- 可選：使用 Google Sheets 作為審核佇列

**簡單實作建議：**
- 初期可以直接返回 `False`（不需要審核）
- 或基於分數閾值決定

---

#### 2. **Reply Engine 中的兩個函數**

##### `select_best_template()` - 🔴 高優先級
**位置：** `x_auto/reply_engine/reply_generator.py:12`

**目的：** 根據關鍵字匹配結果選擇最合適的回覆模板

**當前狀態：**
```python
raise NotImplementedError("Template selection logic is not yet implemented.")
```

**需要實作：**
- 根據匹配的關鍵字選擇對應的模板
- 處理多個匹配時的優先級
- 處理沒有匹配時的預設模板

**實作策略：**
```python
def select_best_template(matches, templates):
    # 1. 如果有 template_id 在 matches 中，直接使用
    # 2. 如果沒有，選擇權重最高的模板
    # 3. 如果沒有匹配，使用預設模板
    # 4. 隨機選擇（避免太predictable）
```

---

##### `build_reply_text()` - 🔴 高優先級
**位置：** `x_auto/reply_engine/reply_generator.py:26`

**目的：** 使用模板和貼文資料生成實際的回覆文字

**當前狀態：**
```python
raise NotImplementedError("Reply rendering is not yet implemented.")
```

**需要實作：**
- 模板變數替換（如 `{name}`, `{handle}`, `{keyword}` 等）
- 可選：使用 OpenAI API 生成更自然的回覆
- 長度檢查（X 限制 280 字元）
- 安全檢查（避免敏感內容）

**實作策略：**
1. **簡單版：** 字串格式化
   ```python
   template_text = template.get("template_text", "")
   return template_text.format(
       handle=post.get("handle", ""),
       name=post.get("name", ""),
       # ...
   )
   ```

2. **進階版：** OpenAI API
   ```python
   prompt = f"Based on this post: {post_text}, write a professional reply using this template: {template_text}"
   # Call OpenAI API
   ```

---

#### 3. **Matcher/Rules 中的函數**

##### `match_keywords()`, `score_post()`, `select_template()` - 🟡 中優先級
**位置：** `x_auto/matcher/rules.py`

**注意：** 這些與 `keyword_matcher.py` 中的函數**重複**了！

**狀態：**
- `keyword_matcher.py` 已實作類似功能
- `rules.py` 中的是早期版本的 stub

**建議：**
- 刪除 `rules.py` 中的重複函數
- 或整合兩個模組的功能

---

#### 4. **Template Builder**

##### `build_reply()`, `validate_reply()` - 🟢 低優先級
**位置：** `x_auto/reply_engine/template_builder.py`

**注意：** 與 `reply_generator.py` 功能重疊

**狀態：**
- `validate_reply()` 已實作（簡單的長度檢查）
- `build_reply()` 未實作但與 `reply_generator.py` 中的 `build_reply_text()` 重複

**建議：**
- 整合到 `reply_generator.py`
- 使用 `validate_reply()` 作為最終檢查

---

## 資料結構缺口分析

### 需要確認的 Google Sheets 結構

#### 1. Keywords Sheet
**目前狀態：** 未確認
**需要欄位：**
```
- keyword (必要)
- weight (必要)
- template_id (可選)
- category (可選)
```

#### 2. Templates Sheet
**目前狀態：** 未確認
**需要欄位：**
```
- template_id (必要)
- template_text (必要)
- category (可選)
- keywords (可選 - 用於匹配)
```

#### 3. Logs Sheet
**目前狀態：** 未創建
**建議欄位：**
```
- timestamp
- profile_handle
- profile_name
- post_id
- post_url
- post_text_preview
- matched_keywords
- score
- template_used
- reply_text
- status (success/failed/pending)
- reply_id (X API 回應的ID)
- error_message
```

---

## 實作優先順序建議

### 🔴 第一階段：核心功能（MVP）

#### Priority 1.1: Reply Generation（最關鍵）
1. **`select_best_template()`**
   - 簡單版：基於 template_id 或關鍵字匹配
   - 時間：1-2 小時

2. **`build_reply_text()`**
   - 簡單版：字串格式化
   - 時間：1 小時

**為什麼優先：** 沒有這個，pipeline 無法完整運行

---

#### Priority 1.2: ID Tracking（避免重複）
3. **`filter_already_processed()`**
   - 使用 Google Sheets 作為存儲
   - 讀取 Logs sheet 中已處理的 post_id
   - 時間：2 小時

**為什麼優先：** 避免重複回覆同一個貼文

---

#### Priority 1.3: Logging（追蹤和除錯）
4. **`format_log_row()`**
   - 定義日誌格式
   - 格式化資料
   - 時間：1 小時

**為什麼優先：** 需要追蹤系統行為和除錯

---

### 🟡 第二階段：資料結構和整合

5. **設置 Keywords 和 Templates Sheets**
   - 創建 sheet 結構
   - 填入測試資料
   - 時間：1 小時

6. **創建 Logs Sheet**
   - 定義欄位結構
   - 設置格式
   - 時間：30 分鐘

7. **持久化 ID Tracking**
   - 改進 `id_tracker.py` 使用 JSON 或 SQLite
   - 或完全依賴 Logs sheet
   - 時間：1-2 小時

---

### 🟢 第三階段：進階功能

8. **`requires_human_approval()`**
   - 實作審核邏輯
   - 可選：Slack/Telegram 整合
   - 時間：2-3 小時

9. **OpenAI 整合**
   - 改進 `build_reply_text()` 使用 AI
   - 更自然的回覆生成
   - 時間：2-3 小時

10. **程式碼清理**
    - 刪除重複的函數（`rules.py`, `template_builder.py`）
    - 整合相似模組
    - 時間：1-2 小時

---

## 預計總時間

- **MVP（第一階段）：** 5-7 小時
- **完整功能（包含第二階段）：** 8-10 小時
- **進階功能（包含第三階段）：** 12-16 小時

---

## 現有測試資料

### Google Sheets - "Researcher" 工作表
- **記錄數：** 49 筆
- **範例資料：**
  ```
  Company: Endeavor Invest
  Name: LafaMarino
  Handle: @LafaMarino
  X (link): https://x.com/LafaMarino
  ```

### 測試流程建議

1. **選擇一個測試帳號**
   - 例如：@LafaMarino

2. **手動創建測試資料**
   - Keywords: 3-5 個關鍵字
   - Templates: 2-3 個模板

3. **運行 POC**
   - 使用 `poc_apify_demo.py` 測試抓取
   - 使用 `reply_once.py` 測試發布

4. **完整 Pipeline 測試**
   - `ENABLE_X_POSTING=false` 先乾跑
   - 檢查日誌輸出
   - 修正問題
   - 啟用發布測試

---

## 建議的實作策略

### 策略 A：最簡單 MVP（推薦）
1. 固定使用一個模板（不需要 template selection）
2. 簡單的字串格式化（不用 OpenAI）
3. 使用 Logs sheet 作為 ID tracking
4. 跳過 human approval

**優點：** 快速驗證整個流程
**時間：** 3-4 小時

---

### 策略 B：完整但簡單的實作
1. 實作所有第一階段功能
2. 使用簡單邏輯（不用 AI）
3. 完整的日誌和追蹤

**優點：** 功能完整但實作簡單
**時間：** 5-7 小時

---

### 策略 C：AI 增強版
1. 完整實作所有功能
2. 整合 OpenAI 生成回覆
3. 人工審核機制

**優點：** 最強大和靈活
**時間：** 12-16 小時

---

## 下一步行動

### 立即可做的事：

1. **決定實作策略** (A, B, or C)
2. **設置測試資料**
   - 創建 Keywords sheet
   - 創建 Templates sheet
   - 創建 Logs sheet
3. **選擇第一個要實作的函數**
4. **開始實作並測試**

---

## 附錄：程式碼品質觀察

### 優點 ✓
- 清晰的模組分離
- 完整的文檔字串
- 良好的錯誤處理（在已實作的部分）
- 使用類型提示

### 可改進之處
- 有重複的函數定義（`rules.py` vs `keyword_matcher.py`）
- `id_tracker` 僅在記憶體中，需要持久化
- 缺少單元測試
- 缺少設定檔的結構說明

---

## 總結

這是一個**結構良好但未完成**的專案。核心基礎設施（API 客戶端、資料存取）都已實作完成，主要缺少的是：

1. 🔴 **回覆生成邏輯**（最關鍵）
2. 🔴 **ID 追蹤過濾**（避免重複）
3. 🟡 **日誌格式化**（除錯和追蹤）
4. 🟡 **Google Sheets 資料結構**（Keywords, Templates, Logs）

建議從**策略 A 或 B** 開始，快速實作一個可運行的 MVP，然後根據實際需求逐步增強功能。
